<div class="container mt-5 d-flex justify-content-center">
  <div class="container col-md-6">
    <div class="d-flex justify-content-between align-items-center">
      <div class="col-3">
        <select name="category" id="category" class="col-12 form-select">
          <option>所有支出</option>
          <option>家居</option>
          <option>交通</option>
          <option>娛樂</option>
          <option>飲食</option>
          <option>其他</option>
        </select>
      </div>
      <div class="mt-3 d-flex flex-column align-items-end col-8">
        <h6 style="font-weight: 600">總金額</h6>
        <h1 style="color:tomato;font-weight: 600;">{{totalAmount}}</h1>
      </div>
    </div>
    <hr>
    {{#each records}}
    <div class="d-flex"> {{!-- expenselist --}}
      <div class="d-flex flex-fill align-items-center px-3 pt-2">
        <i>home</i>
        <div class="ms-2">
          <p class="lead mb-2 p-0" style="font-weight: 500">{{this.name}}</p>
          <p>{{this.date}}</p>
        </div>
        <div class="ms-auto">
          {{this.amount}}元
        </div>
        <a class="btn my-auto" href="records/{{this._id}}/edit">修改</a>
        <form action="./records/{{this._id}}?_method=delete" method="post" class="my-auto">
          <button class="btn">刪除</button>
        </form>
      </div>
    </div>
    {{/each}}
    <hr>
    <div class=" d-flex flex-fill justify-content-center">
      <a class="btn btn-primary mt-4" href="/records/new">新增支出</a>
    </div>
  </div>
</div>